<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BookDetailActivity"
            android:label="책 상세 정보"
            android:parentActivityName=".MainActivity"/> <!-- 뒤로가기 버튼 자동 생성 -->

        <activity android:name=".WishlistActivity"
            android:label="찜한 도서"/>
        <activity android:name=".HistoryActivity" android:label="지난 기록" />
        <activity android:name=".TotalBooksActivity" android:label="전체보기" />
    </application>

</manifest>